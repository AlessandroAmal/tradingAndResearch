# Worker runtime dependencies (Phase 1 + Phase 2/M3)
httpx>=0.27,<0.29
pandas>=2.1,<3.0
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0
APScheduler>=3.10,<4.0
supabase>=2.4,<3.0
yfinance>=0.2.40
lxml>=5.0   # required by yfinance get_earnings_dates (single-stock earnings)

# Phase 2 / M3 — news ingestion + AI layer
feedparser>=6.0,<7.0
anthropic>=0.40,<1.0

# Phase 4 / M9 — local control API (Aggiorna + Analisi AI buttons)
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0

# Dev / test
pytest>=8.0,<9.0
httpx>=0.27,<0.29  # also FastAPI TestClient dependency
