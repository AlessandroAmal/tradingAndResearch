"""Controlled macro-event experiment (paper-only).

Opens HYPOTHETICAL positions at several delays after key US data releases, both
directions, to MEASURE what actually happens — it is evidence-gathering, NOT a
signal generator and NEVER an order (CLAUDE.md: read-only). Every position is
`experiment=true`, kept apart from real risk and from the user's own manual paper
positions so it can't pollute either.
"""
